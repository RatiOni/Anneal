{
  "name": "anneal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "tsc --noEmit && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/db/sessions.test.ts && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/engine/station.test.ts && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/station/view.test.ts"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-sql": "^2.4.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^22.20.2",
    "typescript": "~6.0.3",
    "vite": "^8.0.16"
  },
  "license": "AGPL-3.0-only",
  "description": "A focus tracker that counts the time you actually sat down."
}